# Gender-Detector-CNN
This project implements a Convolutional Neural Network (CNN) to classify gender (Male / Female) from facial images. The model is trained using image data with data augmentation and evaluated using accuracy, confusion matrix, and classification report. The trained model is deployed using FastAPI, Gradio
